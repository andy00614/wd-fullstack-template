# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Database
# 应用运行时使用 (连接池)
DATABASE_URL="postgresql://postgres:password@localhost:5432/wd-fullstack-template"

# 迁移时使用 (直连)
DIRECT_URL="postgresql://postgres:password@localhost:5432/wd-fullstack-template"

# Optional: Site URL for OAuth callbacks (defaults to http://localhost:3000 in dev)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

E2E_TEST_EMAIL=""
E2E_TEST_PASSWORD=""

# Notion Integration (for English Learning Log)
NOTION_API_KEY=""
NOTION_DATABASE_ID="c0ad13f524254c2b9de55eb24e65e4ce"
